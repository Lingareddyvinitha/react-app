import styled from '@emotion/styled';
import tw from 'tailwind.macro';
const Container = styled.div `${tw `flex justify-center  flex-col items-center w-screen bg-gray-300 h-32 flex-grow`}`
const Heading = styled.h3 `${tw `text-2xl font-bold`}`
export { Container, Heading }
