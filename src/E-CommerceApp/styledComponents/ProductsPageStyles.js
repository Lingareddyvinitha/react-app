import styled from '@emotion/styled';
import tw from 'tailwind.macro';

const Container = styled.div `${tw `flex justify-between items-start ` }`

const Group = styled.div `${tw `flex flex-col justify-center w-11/12`}`


export {
    Container,
    Group
}
