import styled from '@emotion/styled';
import tw from 'tailwind.macro';
const Total = styled.div `${tw `flex justify-between items-center`}`

const Label = styled.div `${tw `text-gray-400`}`

const Amount = styled.div `${tw `text-yellow-600`}`

export { Total, Label, Amount }
