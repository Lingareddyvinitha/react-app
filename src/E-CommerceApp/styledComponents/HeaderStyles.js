import styled from '@emotion/styled';
import tw from 'tailwind.macro';

const Container = styled.div `${tw `flex justify-around items-center ` }`

const NoOfProducts = styled.div `${tw ``}`

export {
    Container,
    NoOfProducts
}
